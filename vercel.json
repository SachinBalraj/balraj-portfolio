{
  "buildCommand": "cd businessman-portfolio && npm ci --include=dev && npm run build",
  "outputDirectory": "businessman-portfolio/dist",
  "installCommand": "npm install && cd businessman-portfolio && npm install --include=dev",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
